<li class="list-group-item">
  <%= gravatar_for user, size: 30 %>
  <%= link_to user.name, user %> 
  <% if current_user.admin? && !current_user?(user) %> <%= link_to "Delete", user, class: 'label label-danger pull-right', method: :delete, data: { confirm: "Are You Sure" } %>
  <% end %>
</li>